version: "3.9"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram_bot
    volumes:
      - bot_data:/app
    env_file:
      - .env
    restart: always

volumes:
  bot_data:
